\begin{tikzpicture}[scale=0.5]
    \draw[fill=gray!20] (0,0) rectangle (4,4);
    \draw[step=1cm, gray!50, thin] (0,0) grid (4,4);
    
    % Случайные направления
    \foreach \x in {0.5,1.5,2.5,3.5}
        \foreach \y in {0.5,1.5,2.5,3.5} {
            \pgfmathsetmacro{\angle}{rnd*360}
            \pgfmathsetmacro{\color}{rnd}
            \ifdim\color pt>0.5pt
                \draw[->, thick, red] (\x,\y) -- ++(\angle:0.4);
            \else
                \draw[->, thick, blue] (\x,\y) -- ++(\angle:0.4);
            \fi
        }
    
    % Название сверху
    \node[above, font=\footnotesize, align=center] at (2,4.2) {Спиновое стекло};
    
    % Подпись снизу
    \node[below, font=\scriptsize, align=center] at (2,-0.2) {Случайные\\направления спинов};
    
    % Энергия - опущена ещё ниже
    \node[draw, fill=yellow!20, rounded corners=0.2cm, font=\scriptsize, align=center] at (2,-3.0) {
        $E = -\sum_{<ij>} J_{ij} S_i S_j$, $J_{ij}$ случайны
    };
\end{tikzpicture}